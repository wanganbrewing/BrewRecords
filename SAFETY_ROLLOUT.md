# 第1段階：安全性強化（2026-09-03）

## 今回の範囲

- 同一品目を複数行で使用する場合の合計在庫判定、不正数量・失効した連携先の検知。
- コピー／仕込み量変更コピーで任意工程を保存。工程の実績時刻とIDは引き継がない。
- 同名品目の受払CSV残高を品目ID単位に分離（末尾に品目ID列を追加）。
- 入荷・消費・品目・仕込み・出荷ロットの直前の削除を端末内に退避し、取り消す。
- 未送信データのフラグ、送信中の追加編集の再送、競合時の自動上書き防止。
- 別組織の端末データがある場合は同期を止める。これは組織切り替え機能の代わりではない。
- クラウド保存権限（owner/admin/brewerのみ）と保存前の版を残すSQL更新。

## 公開前の手順

1. 現行アプリからバックアップを取得する。クラウドの現在のrevisionも控える。
2. 検証用Supabaseで `migrations/20260903_safety_foundation.sql` を実行する。
3. 下記のサーバー受入テストを実施する。
4. 本番にも同じSQLを適用してからHTML・cloud-sync.js・help.html・sw.jsを公開する。
5. PCとiPhoneで更新を読み込み、バックアップとログイン状態を確認する。

SQLはトランザクション内で実行し、既存の業務データは削除しない。SQL更新だけでは画面の編集ボタンを閲覧専用にはしないが、サーバーへの保存は拒否する。

## サーバー受入テスト

- owner/admin/brewer：正しいrevisionなら保存が1回成功し、旧版がhistoryに1件残る。
- viewer・組織外ユーザー・未ログイン：保存が拒否され、revisionとhistoryが変化しない。
- 同じrevisionで2回保存：後の保存は競合になり、historyも重複しない。
- 不正payload（batches/inventory欠落や配列以外）：拒否される。
- 他組織のsnapshot/historyを読めない。履歴をクライアントから直接編集・削除できない。
- 実際のPC／スマホでオフライン編集後に他端末が保存しても、端末版が勝手に置き換わらない。

## 今回の制限・次の作業

- これは第1段階の初回改修。全フィードバックの完了ではない。
- 削除取り消しは端末内の直前1操作。削除後の保存・同期などでデータが変わった場合は復元を拒否し、削除前データの書き出しを案内する。フォームの未保存行の削除は対象外。
- 通常の自動バックアップとは別。端末データ消去や故障には備えられない。
- クラウド履歴はSQL適用後の保存から蓄積する。組織単位の版履歴であり、完全な業務監査ログ・法定保存への適合保証ではない。容量監視と保存期間の方針が必要。
- 会社全体のJSON同期は継続。記録単位の同期、入出庫と仕込みをまとめて確定する取引処理、役割別UI、組織切り替えは次の基盤改修。
- ローカルの仕込み・在庫はまだ別々のキーに保存される。端末容量不足・途中終了時の完全な一括保存は次の基盤改修で扱う。
- ロット管理、棚卸調整、タンク・移送・欠減、原価、PC一覧、Excel/PDF、レシピ計算、AI連携は後続段階。

## ローカル検証

`node --test tests/safety.test.cjs`

アプリの実関数と同期コードを検証用環境で実行する。SQLテストは内容の静的確認のみで、PostgreSQLでの実行やRLSテストの代わりではない。

2026-09-03：自動テスト18項目が通過。Browserでローカルデモの起動・品目追加・復元メニュー表示を確認し、コンソールエラーなし。確認ダイアログを取得できず、画面からの削除→復元の一連操作は未完了（関数レベルでは検証済み）。本番公開・本番DBへのSQL適用は未実施。

### 本番DBへの適用結果（2026-09-03、追記）

利用者の了承により別の検証プロジェクトは作成せず、既存プロジェクトで実施。

- `20260903_preflight_backup.sql` でデータ・利用者所属・同期記録・旧保存関数・ポリシーを非公開スキーマへ退避。schema権限を剥奪し、退避テーブルにもRLSを有効化。これは同一DB内の退避であり、障害に備える外部バックアップではない。
- 安全性強化のSQLを適用。Supabaseの既存の個別権限によりanonへの関数実行許可が残っていたため、PUBLICに加えanonからも明示的に剥奪した。
- `tests/server-safety-rollback.sql` が実DB上で通過。owner/admin/brewerの保存と旧版保存、viewer・非所属・未認証の保存拒否、不正payload・古いrevisionの拒否、RLSと直接書き込み権限、退避領域の隔離を確認。
- テストの更新はROLLBACK。アプリデータの内容とrevisionが退避前と一致することを確認した。同期イベントの採番にはテストに伴う欠番が発生し得るが、業務レコードは追加・変更していない。
- 実端末2台での同時編集・オフライン復帰のUATは別途必要。
