# A. 画面・操作性 — 第1回（v63）

## 今回

- 原材料管理の初回表示は横幅1000px以上で在庫一覧、それ未満で品目別。その後はブラウザーの表示選択を優先。リサイズ中に勝手に切り替えない。
- 表示方法は端末のlocalStorageへ保存。デモ・通常版で共通、クラウドやバックアップの業務データには含めない。保存できない環境でも画面を使用可能。
- 記録一覧の検索（名前・スタイル・日付、全半角・大文字小文字を正規化）と状態絞り込み。複数語はAND。件数と該当なしを表示。
- 検索中はカードだけを隠し、入力欄を作り直さない。詳細から戻る時も条件と選択位置を維持。ただし対象の状態が変わって条件外になった場合は解除して対象を表示。
- PC記録一覧は最大1400px・2列。スマホは1列。検索欄は48px以上、文字16px、解除ボタンは44px以上。
- 既存CSVの出力対象は変更しない。検索は表示のみで、記録・在庫・計算・Supabase設定に変更なし。

## 検証

93自動テスト通過。初回表示、保存選択優先、保存不可、検索正規化・状態・空結果・解除・データ非変更・キャッシュを確認。
隔離デモでPC初回在庫一覧、記録検索porterで1件、詳細から戻って条件保持、解除で3件、PC2列のスクリーンショットを確認。検索欄のサイズ不足を修正して再確認。ブラウザーエラーなし。実機iPhoneのUATは未実施。

## 後続

A全体の完了ではない。簡易／詳細モードや項目整理、PCの複数データ同時参照などは別途進める。タンク一覧は業務データ設計を伴うため、この画面だけの改善には含めない。
