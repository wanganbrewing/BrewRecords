# v56：メーカー・ロット／入荷訂正

## 対象

- 品目に任意の manufacturer、lotCode を追加。既存データは空欄として扱い、自動分割しない。
- 同じ分類・品目名・メーカー・ロット・単位の重複登録は拒否。大小文字・前後空白を正規化して比較する。既存の重複は統合しない。
- 別ロットは別品目ID。仕込みの在庫選択肢にもメーカー・ロットを表示。消費連携は既存のIDのまま。
- メーカー・ロット情報の訂正は、その品目全体に適用。理由・訂正前後・時刻を metadataHistory に保存。
- 入荷の追加／訂正を共通ダイアログ化。日付・数量・仕入れ先・金額を入力し、現在庫の変化を確認する。任意金額の空欄と0を区別。
- 入荷訂正は同じ receipt.id を保持。corrections に理由・前後の値・時刻を追記。二重の入荷行を作らず、消費・棚卸差分を変更しない。
- 訂正により負在庫になる場合は警告して確認。実際の入荷数量への訂正を妨げず、不足分を架空の入荷や調整で埋めない。
- 品目カード・現在庫一覧・在庫選択肢を消費済みの異なる仕込み数の降順にする。名前順へ変更可。受払一覧は日付順。
- 在庫／受払一覧でメーカー・ロット検索。CSV末尾にメーカー・ロットと訂正履歴JSON列を追加、既存14列の順序は保持。

## 安全性

入力開始時と確認後に全データのスナップショットを比較し、変更があれば保存を中止。二重押しをガード。端末保存に失敗した場合は差し替えた在庫配列を復元する。保存後は既存の自動バックアップ／クラウド保存待ち処理へ渡す。実運用のデータ・Supabase設定は変更しない。

既存JSONバックアップとクラウドのスナップショットは任意プロパティを保持するため、DBの変更は不要。旧版の画面は新フィールドを表示できないため、両端末でv56以降を使用する。

## 検証

`node --test tests/safety.test.cjs tests/inventory-lots.test.cjs`

既存29件＋追加9件：別ロットの独立性、使用回数、入荷訂正／監査履歴、入力検証、キャンセル、競合、二重保存、保存失敗、CSV、負在庫警告、HTMLエスケープ。

ローカルの隔離デモで品目登録、入荷確認キャンセル、入荷保存、数量／金額0の訂正、履歴表示、検索、再読み込み後の保持を確認。実機iPhoneと実ユーザーの端末間同期は今回未検証。

## 次段階

この版は同一品目内でのロット混在・自動先入先出・既存在庫の自動分割を扱わない。過去の棚卸時点の残量は監査用に保持されるが、古い入荷訂正で現在庫は再計算される。過去時点の在庫評価額固定、月末評価、原価集計は別段階。ローカルとクラウドを跨ぐ完全なトランザクション、監査ログの改ざん防止、権限による訂正制限は別設計が必要。
