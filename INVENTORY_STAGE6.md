# v59：消耗品参考単価と仕込み使用量

## 操作場所

- 原材料管理 → 消耗品・参考単価 → ＋ 参考単価を登録。
- 仕込み → 消耗品・参考費用 → 消耗品の使用量・その他費用を入力。
- 品目を追加して使用量を入力 → 内容を確認 → 仕込みへ反映 → 仕込み画面の保存する。
- 記録一覧 → 詳細 → 原価（参考）。従来の手入力費用も保持。

## データと計算

costCatalog={items,history}をwangan-cost-catalogへ保存。単価変更は理由・変更前後を記録。完全削除ではなく選択肢から非表示にできる。分類と同名・同単位の有効品目は重複を禁止。単位mL/L/g/kg/個/回（1仕込み）、単価と使用量は小数第4位まで。0円・0量は有効、空欄は未計算。

仕込みのotherCosts[].pricingにcatalogId/revision/unit/rate/quantityを保持。参考費用は保存した単価×使用量を小数第2位に丸める。参考行のamountやpercentを流用して二重配分せず、常にpricingから計算。共通単価を編集しても過去の費用は変えない。同一catalogIdの行は1つにまとめる。1仕込み単位は使用量1を初期値とする。単位換算・薬剤の使用量提案はしない。

参考費用は原材料費とは別に集計する。従来の金額×割合方式は手入力費用として保持し、勝手に置換しない。手入力と参考行にまたがる同一費用の意味的な重複は自動検出しない。消耗品の在庫控除・受払簿・月末棚卸評価は対象外。原価合計は参考値で、会計・申告用確定額ではない。

新規／編集中の仕込みでは費用ダイアログで即保存せずformExpenseDraftへ反映し、仕込みの通常保存で確定する。キャンセル／新規作成／別記録の編集で草稿をクリア。入力後に全体スナップショットが変わった場合は保存を中止。参考単価更新は保存確認前後の競合チェック、二重押し防止、保存失敗時に元データを保持。仕込みコピーはv58同様、費用を引き継がない。

## バックアップ・同期

JSONバックアップ、日次バックアップ、全体復元、クラウドスナップショットに共通単価を含む。単価だけ登録した状態でもバックアップ可能。追加インポートは同じIDの既存単価を優先し、未登録IDと履歴を追加。置換復元はバックアップの単価を使う。旧形式で単価が欠けたクラウド受信は、同じワークスペース内の端末の単価を保持して再送する。旧クライアントのバッチ編集がpricingを落とす可能性はあるため全端末v59以上を推奨。サーバー側の最低版強制は未実装。

DBスキーマ・認証設定は変更しない。複数キーの全体復元は従来同様、完全なトランザクションではない。履歴は変更追跡であり改ざん不能な監査証跡ではない。CSVは費用明細JSONと参考単価による概算小計を含む。

## 検証

75件の自動テスト通過：既存在庫・原価・同期回帰、単価検証、単価変更前後、非表示、重複拒否、固定単価計算、空欄と0、1仕込み単位、バックアップ統合、旧クラウド互換、保存失敗・キャンセル・二重押し・競合、仕込み費用草稿保持と競合防止。

ローカル隔離デモで単価2,000円/Lを登録し、新規仕込みに0.1Lを入力して200円を保存。共通単価3,000円へ変更し再読込後も、過去の明細2,000円/L×0.1Lを維持。同一消耗品の重複追加を拒否。ブラウザーのエラーログなし。実ユーザーデータ・Supabase設定は未変更。実機iPhone・実2端末同時同期は未実施。
