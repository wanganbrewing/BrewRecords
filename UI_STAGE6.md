# v71 追加工程の入力案内

- 数値増減ボタンを作る前にラベルを関連付け、プレースホルダーの「15」でなく「所要時間（分）」を案内。
- 動的入力欄のIDを既存uid関数で生成。同時に追加した別行でIDが重なるリスクを減らす。
- 隔離デモで追加工程を3行表示し15入力のIDがすべて異なること、各行の案内、390px幅のはみ出しなしを確認。ブラウザーエラーなし。
- 保存処理・ユーザーデータは変更なし。実機iPhoneの音声読み上げは未検証。
