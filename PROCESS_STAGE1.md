# v61：工程ごとの実測記録

## 操作

- 仕込み時間割 → 対象バッチ → 工程ごとの実測記録 → ＋ 実測記録を追加。
- 工程名は保存済みの標準・追加工程から選択、または自由入力。同じ工程に複数回登録可能。
- 測定日必須、時刻任意。SG・pH・温度℃・液量Lはいずれか1項目以上。空欄は未測定、0は実測値。
- 登録・訂正理由必須。確認後に保存。訂正は同じ記録IDを維持し、変更前後と理由を履歴に保存。
- 記録詳細にも表示。すべての実測記録を☰ → データの書き出し → 工程実測記録CSVから出力。

## 範囲と安全性

- 各バッチにprocessMeasurements配列を保持。実測ログは独立した記録で、工程名は測定時の名称を保持。工程の改名・削除で過去の測定値を消さない。
- 在庫控除・タンク移送・欠減の記録ではない。OG、発酵ログ、タンク残量へ自動転記しない。SGの温度補正も行わない。
- 比重0.8〜1.3（小数3位）、pH0〜14（2位）、温度-10〜150℃（1位）、液量0〜1000万L（3位）。入力の取り違え検出用の範囲で推奨醸造条件ではない。
- 1バッチ1000記録まで。既存データへ自動で測定値を補完しない。コピー・倍率コピーでは測定値と履歴を初期化、通常編集では保持。
- 保存確認前後に全体スナップショットを比較。二重押しを防ぎ、保存失敗時は差し替えたメモリを復元する。時間割の未保存の追加工程を再描画しない。
- 既存のJSONバックアップ・クラウド同期に含まれる。DB変更なし。旧クライアントの編集はフィールドを落とす可能性があるため全端末v61以降を推奨。
- 履歴は改ざん不能な監査証跡ではない。記録の削除機能は今回追加しない。全フィードバック・タンク管理の完成ではない。

## 検証

88項目の自動テスト通過。実測値の検証、未入力と0、同工程への複数追加、訂正と不変履歴、競合・キャンセル・保存失敗・二重押し、CSV、エスケープ、バックアップと実スナップショット往復、コピー初期化を確認。
隔離デモでSG1.042・pH5.2・65℃・20Lを保存、pH5.3へ訂正し前後履歴を確認。再読み込みで保持、CSVダウンロードの内容一致、ブラウザーのエラーログなし。実ユーザーデータ・Supabase設定は変更しない。
実機iPhone・2台の実端末による同時同期テストは未実施。
