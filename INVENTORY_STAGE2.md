# 第2段階（初回）：在庫一覧・受払一覧・棚卸調整

2026-09-03 / v54

## 実装

- 原材料管理に「品目別」「在庫一覧」「受払一覧」の切り替えを追加。分類・品目名で検索。受払は日付範囲でも検索。
- PCの一覧は最大1400px幅。表内スクロール・固定見出し。小画面では表を横スクロール。
- 棚卸調整で現在の実在庫と理由を入力し、差分を確認後に保存。0を許可、小数第3位まで。空欄・負値・非有限値・理由なし・差分なし・未来日付の既存履歴を拒否。
- 品目の `adjustments` 配列に `{id,date,recordedAt,amount,before,actual,reason}` を追加。差分amountのみ在庫に加算。元の入荷・消費・レシピは変更しない。
- 入力中／確認中にデータが変われば中止。二重送信を防止。端末保存失敗時は今回の調整を戻してエラー表示。
- 在庫消費判定と在庫一覧は既存のinvStockを共用。受払とCSVはgetInventoryLedgerRowsを共用。
- CSVの既存先頭10列を維持し、調整理由・当時の理論在庫・実在庫・記録日時を追記。画面の絞り込みとは独立して全件を書き出す。
- JSONバックアップ／クラウドの既存snapshotにadjustmentsを含める。追加SQL不要。既存の業務データを一括変換しない。
- 使い方と？ヘルプも更新。

## 表示・運用ルール

- 在庫は登録済みの全履歴を合算。受払の期間絞り込みは残高計算後に行う。
- 同日内の表示順は入荷→消費→調整。時刻未記録の既存履歴を含むため、実際の作業順の完全再現ではない。
- 調整は本日付のみ。誤調整は正しい実在庫で再調整する。後から旧入荷・消費を変更すれば現在庫も再計算されるが、調整時のbefore/actualは変えない。
- この履歴は完全な監査証跡や法定帳簿への適合を保証するものではない。品目全体の削除ではその品目の調整も削除される。
- 全端末で更新して使用すること。古い版は調整差分を残量へ加算しない。

## 検証

- node --test tests/safety.test.cjs：27件成功。
- ローカルデモの実操作：一覧検索、棚卸入力・差分確認・キャンセル・保存、4.65kg→4.5kgの表示、再読み込み後の保持、期間絞り込みでの残高保持、他メニューへの切り替え。コンソールエラーなし。
- 通常版の実在庫は変更していない。実機iPhoneとPC間の同期UATは未実施。

## 後続（今回の完了範囲外）

- メーカー／ロット属性と入荷内容の訂正、利用頻度順、月末棚卸数量・評価額。
- 仕込みと在庫の一括永続化、レコード単位同期・権限別画面など、SAFETY_ROLLOUT.md記載の基盤課題。
- タンク・移送・欠減・原価・Excel/PDF・レシピ予測・AI取り込み。
