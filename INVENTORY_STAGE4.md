# v57：月末棚卸参考額・消費原材料費

## 操作場所

- 原材料管理 → 月末棚卸額（参考）。初期状態は閉じる。
- 記録一覧 → 対象記録 → 原材料費（参考）。初期状態は閉じる。
- ☰ → データの書き出し → 月末棚卸参考額CSV。表示中の結果を出力。

## 計算範囲

inventory-costing.jsの純粋関数で、品目ID（メーカー・ロット別）単位に移動平均を計算。同日は既存受払簿と同じ入荷→消費→調整の順。入荷合計金額を在庫価額に加算し、消費／調整はその時点の平均単価で減増。計算途中の平均単価は丸めず、品目価額を小数第2位で丸めて合計する。

未来の取引は集計日から除外。不正日付は所属月を判定できないためその品目を未計算とする。未入力金額・不正金額・負在庫はnullで扱う。明示の0円と未入力を区別。在庫がちょうど0になった後、新たな有償入荷から単価計算を再開できる。過去の未計算消費は未計算のまま。残量0・価額0の品目は単価を「—」表示。

原材料費は消費履歴から集計し、レシピの連携ID別合計数量と一致を確認する。未連携／未消費／数量不一致／不明単価は未確定。仕込み方法の変更だけで在庫を自動消費しない。税区分は変換せず入荷金額をそのまま使う。洗浄剤・光熱費・包装費・人件費・水質添加剤は含まない。会社の正式な会計評価・申告額や完全な製造原価とは扱わない。

## 保存と自動処理

月末を過ぎた月のみ、理由付きで結果の独立コピーを保存。当月は今日までの参考表示。保存済み結果を変更せず、現在の履歴を再計算した訂正版を追記する。同じ結果の連続保存を抑止。

自動保存は初期OFF。ONにした月から、翌月以降のアプリ起動時または月末画面を開いた時に、未保存月を現時点の履歴から集計する。アプリ終了中の定時実行ではない。欠落した取引・過去に削除した品目を復元する機能ではなく、実際の月末現物数量の証明ではない。既存の月末結果は自動で訂正しない。

新規トップレベルvaluationBook={reports,autoEnabled,startMonth}をwangan-valuationsへ保存。JSONバックアップ・日次バックアップ・クラウドスナップショットに含める。追加インポートでは保存結果のIDで重複排除し自動設定は端末側を維持。置換／復元ではバックアップの設定を使い、旧形式なら空の月末データとする。

クラウド受信時は同じワークスペース内の保存済みアーカイブをIDで合流し、旧版から消えたフィールドが端末の月末履歴を消さないように再送する。新しい端末に旧クラウド内容しか存在しない場合、他の端末やバックアップがなければ過去アーカイブを復元できない。全端末v57以降で使用すること。サーバーの最小クライアント版強制・改ざん防止は未導入。

保存確認前後のスナップショット検査、二重押し防止、月末データ単独保存失敗時の復元を実装。既存の複数キーにまたがる全データの完全な一括保存は未実装。Supabaseのスキーマ変更はなし。JSONスナップショットに追加フィールドとして保持。

## 検証

`node --test tests/safety.test.cjs tests/inventory-lots.test.cjs tests/inventory-costing.test.cjs`

55件：従来の在庫・削除安全性、版番号、同期、移動平均、未入力と0、日付境界、月末結果の独立性、自動保存の重複抑止、保存失敗、競合中断、実スナップショット／JSONバックアップの往復、旧クラウドからのアーカイブ保全・再送。

ローカル隔離デモで8月末83,614円を保存、キャンセル、再読み込み後に保存版を再表示。夏みかんセゾンの消費原材料費3,564.40円と4明細を確認。実際の本番データ・Supabase設定は編集しない。実機iPhoneと2端末同時同期のUATは未実施。
